The AddToCart component acts like a quantity selector.

Once the user selected a value in the select field & clicked the form, the `onAddToCartRequest` event is triggered.

```js { "props": { "stock": 10 } }
<AddToCart stock={10} />
```